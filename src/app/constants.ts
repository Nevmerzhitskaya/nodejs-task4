export const enum StatusCode {
    OK = 200,
    BAD_REQUEST = 400,
    NOT_FOUND = 404,
    CREATED = 201,
    NO_CONTENT = 204
}

export const enum ErrorMessage {
    USER_NOT_FOUND = "User not found",
}